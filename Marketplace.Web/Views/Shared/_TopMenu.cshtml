<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="@Url.Action("Index","Home")"><i class="fas fa-store" style="color:green"></i> Marketplace </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index","Auctions")"><i class="fas fa-tag mr-2" style="color:green"></i>Auctions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index","Categories")"><i class="fas fa-sitemap mr-2" style="color:green"></i>Categories</a>
                </li>



                @if (User.Identity.IsAuthenticated)
                {
                    if (User.IsInRole("Administrator"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Dashboard")"><i class="far fa-caret-square-down mr-2" style="color:green"></i>Dashboard</a>
                        </li>
                    }

                    <li class="nav-item">
                        <a class="nav-link" id="btnLogOff" href="#"><i class="fas fa-sign-out-alt mr-2" style="color:green"></i>LogOff</a>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Login", "Account")"><i class="fas fa-sign-out-alt mr-2" style="color:green"></i>Login</a>
                    </li>
                }

            </ul>
        </div>
    </div>
</nav>


<form id="logOffForm" method="post" action="@Url.Action("LogOff", "Account")">
    @Html.AntiForgeryToken()
</form>

    <script>
        $("#btnLogOff").click(function () {
            $("#logOffForm").submit();
        });

    </script>
