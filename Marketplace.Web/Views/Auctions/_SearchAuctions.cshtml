@*@model Marketplace.Web.ViewModels.AuctionsListingViewModel

    <div class="row">
        <div id="auctionsList" class="col-lg-12">
            @if (Model.Auctions != null && Model.Auctions.Count > 0)
            {
                for (int i = 1; i <= Model.Auctions.Count; i++)
                {

                    i--;
                    var groupItem = 0;
                    var classes = i == 0 ? "border-top-0 rounded-0" : string.Empty;
                }
                <div class="card-group mb-4">
                    @foreach (var auction in Model.Auctions.Skip(i).Take(3))
                    {
                        groupItem++;
                        var picURL = auction.AuctionPictures != null && auction.AuctionPictures.Count > 0 ?
                            "/Content/images/" + auction.AuctionPictures.First().Picture.URL : "http://placehold.it/900x350";

                        <div class="card @classes" relf-item="@groupItem">
                            <a href="@Url.Action("Details", "Auctions", new { category = auction.Category.Name.ToLower(), ID = auction.ID })">
                                <img class="card-img-top" src="@picURL" alt="@auction.Title" style="height:300px;" />
                            </a>
                            <hr />
                            <div class="card-body">
                                <h5 class="card-title">
                                    <a href="@Url.Action("Details", "Auctions", new { category = auction.Category.Name.ToLower(), ID = auction.ID })"></a>
                                    <span class="badge badge-success" text-white>New</span>
                                </h5>
                                <p class="card-text">
                                    @(!string.IsNullOrEmpty(auction.Summary) && auction.Summary.Length >= 200 ? auction)
                                </p>
                            </div>
                        </div>
                    }
                </div>
                <ul class="list-group list-group flush">
                    <li class="list-group-item border-bottom-0">
                        <i class="fas fa-tags mr-1"></i>
                    </li>
                </ul>


            }

        </div>
    </div>*@













